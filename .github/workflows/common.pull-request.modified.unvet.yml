name: "Pull Request / Modified: Unvet"

on:
  workflow_run:
    workflows:
      - "Pull Request / Modified"
    types:
      - completed

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  pull-request:
    name: "Details"
    uses: auth0/auth0-PHP/.github/workflows/common.pr-details.get.yml@main

  check:
    needs: pull-request

    name: "Run"
    runs-on: ubuntu-latest

    steps:
      - if: ${{ github.actor == 'dependabot[bot]' }}
        run: exit 0

      - if: ${{ ! contains(github.event.pull_request.labels.*.name, 'Vetted') }}
        run: exit 0

      - uses: actions/checkout@v3

      - uses: actions/github-script@v6
        with:
          script: |
            github.rest.issues.removeLabel({
              issue_number: '${{ jobs.pull-request.outputs.number }}',
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: ["Vetted"]
            })
