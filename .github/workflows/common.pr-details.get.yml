name: Get Pull Request Details

on:
  workflow_call:
    outputs:
      number:
        value: ${{ jobs.get.outputs.pr_number }}
      sha:
        value: ${{ jobs.get.outputs.pr_sha }}

jobs:
  get:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - run: |
          RUN_ID=`gh run --repo ${OTHER_REPO} list --workflow common.pr-details.save.yml --json databaseId --jq .[0].databaseId`
          gh run --repo ${OTHER_REPO} download ${RUN_ID} -n pr_details
          # List the artifiact directory/files
          ls -lR
          unzip pr_details.zip

      # - uses: actions/download-artifact@v3
      #   with:
      #     name: pr_details

      # - run: unzip pr_details.zip

      - uses: actions/github-script@v6
        with:
          script: |
            let fs = require('fs');
            let number = Number(fs.readFileSync('./number'));
            let sha = fs.readFileSync('./sha', 'utf-8');

            core.setOutput('number', number);
            core.setOutput('sha', sha);

      - run: exit 0
